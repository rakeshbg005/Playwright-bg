{
  "name": "playwrightdash",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npx playwright install && npm install --save-dev @types/node",
    "bddtest": "cross-env ENVIRONMENT=int npx bddgen && npx playwright test --grep @test1 --workers=1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@types/node": "^25.6.0",
    "allure-playwright": "^3.6.0",
    "cross-env": "^10.1.0",
    "playwright-bdd": "^8.5.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
